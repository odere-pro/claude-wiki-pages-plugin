# Bun project config — substitutes for vitest.config.ts / tsup.config.ts in the
# agentline layout. `bun test` discovers *.test.ts colocated under src/.
[test]
coverage = false
coverageThreshold = { line = 0.8, function = 0.8, statement = 0.8 }
