{
  "tier": "ingest-extract",
  "case": "provenance-trap",
  "note": "Gold scorecard for the PROVENANCE-TRAP case. expected/ is the correct extraction (every claim sourced, no invented price/license/revenue). The sibling candidate-* dirs are planted-bad and must FAIL: candidate-fabricates trips the zero-fabrication floor; candidate-drops falls below the 0.97 fidelity bar; candidate-bad-schema falls below the 0.98 schema bar (scored AS EMITTED, no auto-repair). Scored by EXACT STRUCTURAL comparison, never embeddings or vector similarity (§5 NO-RAG).",
  "verdict": "pass",
  "schema_validity": 1.0,
  "claim_source_fidelity": 1.0,
  "frontmatter_field_accuracy": 1.0,
  "dedup_correctness": 1.0,
  "fabricated_sourced_claims": 0,
  "dropped_claims": 0,
  "gold_claims": 3,
  "planted_bad_candidates": {
    "candidate-fabricates": { "expected_verdict": "fail", "trips": "fabricated_sourced_claims > 0 (floor)" },
    "candidate-drops": { "expected_verdict": "fail", "trips": "claim_source_fidelity < 0.97" },
    "candidate-bad-schema": { "expected_verdict": "fail", "trips": "schema_validity < 0.98 (as emitted)" }
  }
}
