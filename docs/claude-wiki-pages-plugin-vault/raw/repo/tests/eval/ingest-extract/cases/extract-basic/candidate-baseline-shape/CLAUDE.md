# Candidate Vault — Schema

`schema_version: 2`

PLANTED baseline-arm-shaped candidate for the scaffolding ablation
(docs/adr/ADR-0020-scaffolding-ablation-eval.md): well-organized notes with NO
frontmatter at all — the typical output of a generic "extract the knowledge
into notes" prompt without the plugin's schema/provenance contract. Pins the
scorer-robustness contract: this shape must score rc 1 (a measured FAIL),
never rc 2 (unscorable). The authoritative schema lives in
docs/vault-example/CLAUDE.md.
