# Four-Layer Architecture

Layer 1 — Data holds the vault content.
Layer 2 — Skills provide single-responsibility capabilities.
Layer 3 — Agents execute multi-step workflows.
Layer 4 — Orchestration connects everything via hooks and scripts.

Use `/claude-wiki-pages:wiki` to access the wiki.
Use `/claude-wiki-pages:init` to initialise a vault.
